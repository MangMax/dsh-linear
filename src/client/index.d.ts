/**
 * Type stub for the browser half (the harness loads it as a classic script
 * via `exports["./client"]`; the types mirror the client plugin surface).
 */
export declare const inject: string[];
export declare function apply(ctx: unknown): void;
